mapping:
  type: object
  properties:
    type:
      type: string
    options:
      type: object
      properties:
        from:
          type: string
        match:
          type: string
        pattern:
          type: string
        to:
          type: string
        result:
          type: object
          properties:
            index:
              type: integer
            text:
              type: string
