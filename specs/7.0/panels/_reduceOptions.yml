reduceOptions:
  type: object
  properties:
    calcs:
      type: array
      items:
        type: string
      default:
        - mean
    fields:
      type: string
    values:
      type: boolean
      default: false